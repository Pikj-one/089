[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vulnhunt"
version = "0.9.4"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
vulnhunt = "vulnhunt.main:main"

[tool.setuptools.packages.find]
where = ["src"]
