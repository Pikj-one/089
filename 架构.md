# 前言

本项目是编排本地ClaudeCode CLI和Codex CLI的自动化工作流

项目不负责记忆、安全边界等,依赖于原生CLI能力,本项目只负责编排、CLI生命周期管理等

项目前期采用Py＋极简TUI(">"光标＋运行时＋logs打印)作为MVP版,后续迁移至TS＋前端ReAct＋Vite

## 核心设计目标

系统需要满足以下目标：

### 1.1 自动化

用户提交一个漏洞挖掘任务后，系统能够自动启动 Claude Code 进行规划，并根据 Plan 自动产生 Codex 并发任务。

### 1.2 闭环

执行结果必须重新进入 Claude Code 的分析上下文，使其能够发现遗漏、调整策略并生成下一轮 Plan。

### 1.3 并发

一个 Plan 可以拆分为多个相互独立的 Codex 任务，由 Worker Pool 并发执行，提高整体吞吐量。

### 1.4 可恢复

任意一个 Worker 失败、超时、退出或机器重启后，系统应能够从持久化状态恢复，而不必从头开始。

### 1.5 可观测

系统需要保存每一轮 Plan、每个 Codex Task、执行日志、发现结果、证据以及最终报告，使整个过程可以审计和复盘。

### 1.6 自动完成判定

是否完成不能完全依赖 Claude Code 自己给出的 `COMPLETE` 信号，而应由 Orchestrator 根据任务覆盖度、剩余高优先级任务、结果质量和循环状态独立判断。

## Agent 职责划分

### 2.1 Claude Code

Claude Code 在系统中承担 Planner / Reviewer 角色。

主要职责：

1. 理解用户提交的目标和约束。
2. 分析目标的攻击面或审计面。
3. 将复杂任务拆分为可并行执行的子任务。
4. 为任务定义优先级和预期产出。
5. 分析上一轮 Codex 的执行结果。
6. 判断哪些方向已经覆盖、哪些方向需要进一步验证。
7. 生成下一轮 Plan。
8. 在满足完成条件时输出完成意图。

Claude Code 不直接作为主要执行 Worker。

其核心交互模型为：

```
Input Context
     ↓
Claude Code Plan Mode
     ↓
Plan File
```

### 2.2 Codex CLI

Codex CLI 承担具体 Worker 角色。

每个 Codex Worker 接收到：

```
Task Description
Workspace
Required Output
Relevant Context
```

然后在 Codex CLI 自身的 Sandbox 机制下执行。

系统不修改 Codex 的内部安全策略，也不尝试绕过其沙箱限制。

其职责主要包括：

1. 完成指定分析任务。
2. 执行允许的测试和验证。
3. 收集可复现证据。
4. 输出结构化结果。
5. 返回成功、失败、超时等执行状态。

### 2.3 Orchestrator

Orchestrator 是整个系统的核心控制层。

主要负责：

```
任务生命周期
Agent 调度
CLI 进程生命周期
并发控制
结果收集
状态持久化
失败恢复
轮次管理
完成判定
```

它不负责替 Claude Code 进行复杂漏洞分析，也不负责替 Codex 实施实际测试。