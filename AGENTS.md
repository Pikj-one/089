# 项目开发指南
> 约定：代码即文档能用注释和类型定义说清的，绝不在 docs/ 里重复——比如函数参数说明。docs/ 只记录“为什么”和“怎么联调”，不记录“怎么用API”。

## 项目结构
按功能模块组织，而不是把所有的控制器、模型、工具类全扔在一起。
> 原则：能放模块里就放模块里，只有真正被3个以上模块复用的代码才往上提

## 版本号规则
采用 语义化版本（SemVer），格式为 主版本号.次版本号.补丁号（如 v2.1.3）。
  - 主版本号：不兼容的API大改（如升级架构）时递增。
  - 次版本号：新增功能但向下兼容时递增。
  - 补丁号：修Bug或小优化时递增。

## Git规范
强制用 约定式提交（Conventional Commits） 格式，直接对接语义化版本：
```
<类型>(<范围>): <简短描述>
  
<详细说明>
<关联Issue>
```
类型用 feat（新功能）、fix（修Bug）、docs（文档）、refactor（重构）、perf（性能）、chore（构建工具）等。范围填模块名，比如 feat(user): 增加手机号登录。
> 终端ssh已绑定github邮箱，仓库`git@github.com:Pikj-one/089.git`